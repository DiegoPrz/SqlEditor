{
  "name": "sqleditor",
  "version": "0.0.1",
  "description": "Sql editor for Oracle database",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "start": "npm run build && electron ./dist/electron-app.js",
    "test": "jest"
  },
  "author": "Diego Perez",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "electron": "^18.3.7",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.19.0",
    "typescript": "^3.6.2"
  }
}
